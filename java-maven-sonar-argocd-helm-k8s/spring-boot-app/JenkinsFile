pipeline {
    agent any

    stages {
        stage('Syntax Test') {
            steps {
                sh '''
                    echo "Jenkinsfile syntax is OK"
                '''
            }
        }
    }
}
